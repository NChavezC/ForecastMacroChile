# Forecast Macro Chile

A simple WebApp that lets the user choose a macroeconomic series from Chile and a model to predict its future values, and then creates a forecast based on the model selected. The user can download the forecast or the original data + the forecast in a csv file
